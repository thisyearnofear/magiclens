{
  "installCommand": "cd app && pnpm install",
  "buildCommand": "cd app && pnpm run build:prod",
  "outputDirectory": "app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}